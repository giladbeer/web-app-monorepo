{
  "name": "web-app-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "turbo": "^1.12.4",
    "@biomejs/biome": "^1.5.3"
  },
  "packageManager": "pnpm@8.6.10",
  "engines": {
    "node": ">=22"
  }
}